function parallax(element, distance, speed){
    const item =document.querySelector(element);

    item.style.transform
}

window.addEventListener('scroll', function(){
    parallax('header',window,1);
})
